import React, { useEffect } from 'react'
import { connect } from 'react-redux'

function Delete(props) {
  //props.match.params.id


}

// if I plug the match id into the Axios.delete('url/smurfs/id')
// it should work right?