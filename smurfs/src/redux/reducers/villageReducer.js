const initialState = {
  villagers: [],
  isFetching: false,
  error: false
}

export const villageReducer = (state = initialState, { type, payload }) => {
  switch(type) {

    default:
      return state
  }
}